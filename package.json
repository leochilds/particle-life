{
  "name": "particle-life-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static web viewer for Particle Life (docs/). The Python package lives alongside it.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000 --directory docs"
  },
  "license": "MIT"
}
